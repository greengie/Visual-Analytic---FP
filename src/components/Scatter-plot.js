import React, { Component } from "react";
import d3 from 'd3';

class plotScatter extends Component{
  constructor(props) {
    super(props);
    console.log(props);
  }

  render(){
    return(
      <h4>Scatter-Plot</h4>
    );
  }
}

export default plotScatter;
